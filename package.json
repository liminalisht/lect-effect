{
  "name": "lect-effect",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/liminalisht/lect-effect",
  "main": "dist/src/index.js",
  "type": "module",
  "scripts": {
    "lect-effect/build": "pnpm -C packages/domain build && pnpm -C apps/backend build && pnpm -C apps/frontend build && pnpm -C docs build",
    "lect-effect/clean": "pnpm -C packages/domain clean && pnpm -C apps/backend clean && pnpm -C apps/frontend clean && pnpm -C docs clean",
    "lect-effect/docs:generate": "pnpm -C apps/backend docs:generate && pnpm -C packages/domain docs:generate && pnpm -C apps/frontend docs:generate",
    "lect-effect/install": "pnpm install --frozen-lockfile",
    "lect-effect/install:nofreeze": "pnpm install --no-frozen-lockfile",
    "lect-effect/lint": "pnpx xo --ignore 'docs-site/**'",
    "lect-effect/migrate/masterdata:up": "env-cmd -f .env --no-override node-pg-migrate --migration-filename-format utc -d MASTERDATA_PG_URL -m apps/backend/migrations/masterdatadb up",
    "lect-effect/migrate/test-masterdata:up": "env-cmd -f .env.test --no-override node-pg-migrate --migration-filename-format utc -d TEST_MASTERDATA_PG_URL -m apps/backend/migrations/masterdatadb up",
    "lect-effect/start": "concurrently --names backend,frontend --prefix-colors blue,green \"pnpm -C apps/backend start\" \"pnpm -C apps/frontend start\"",
    "lect-effect/start:with-docs": "concurrently --names backend,frontend,docs --prefix-colors blue,green,magenta \"pnpm -C apps/backend start\" \"pnpm -C apps/frontend start\" \"pnpm -C docs dev\"",
    "lect-effect/test": "pnpm -C packages/domain test && pnpm -C apps/backend test && pnpm -C apps/frontend test"
  },
  "keywords": [],
  "author": "Abraham Howland",
  "license": "MIT",
  "packageManager": "pnpm@10.22.0",
  "volta": {
    "node": "24.12.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@effect/language-service": "^0.64.1",
    "@tsconfig/node24": "^24.0.3",
    "@types/node": "^25.0.3",
    "concurrently": "^9.0.0",
    "env-cmd": "^11.0.0",
    "node-pg-migrate": "^8.0.4",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "xo": "^1.2.3"
  }
}
