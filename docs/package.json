{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf dist .astro",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "astro sync",
    "install": "pnpm --dir .. install --filter ./docs --frozen-lockfile",
    "install:nofreeze": "pnpm --dir .. install --filter ./docs --no-frozen-lockfile",

    "clean:build": "pnpm run clean && pnpm run build",
    "predev": "pnpm run sync",
    "prestart": "pnpm run sync",
    "prebuild": "pnpm run sync",
    "prepreview": "pnpm run sync",

    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.2",
    "astro": "^5.6.1",
    "sharp": "^0.34.2",
    "starlight-theme-gruvbox": "^1.0.0"
  }
}
