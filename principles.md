# Data

Use Effect's `Schema` to create schemas, do branding, etc. This gives us branding and validation/derivation (decode, encode, arbitrary) in one place, and maximizes our ability to derive types, arbitrary data, etc. from the data schema.
